# 常见问题 (Frequently Asked Questions)

本文档汇总了 BurpAI Agent 的常见问题和解决方案。

## 目录

- [安装与配置](#安装与配置)
- [LLM 相关](#llm-相关)
- [功能使用](#功能使用)
- [性能与优化](#性能与优化)
- [错误排查](#错误排查)
- [安全与隐私](#安全与隐私)

---

## 安装与配置

### Q1: 支持哪些 Burp Suite 版本?

**A**: BurpAI Agent 需要 **Burp Suite Professional 2023.x 或更高版本**。

- ✅ 支持: Professional Edition 2023.1+
- ❌ 不支持: Community Edition (功能受限)
- ⚠️ 旧版本 (2022.x): 未测试，可能存在兼容性问题

### Q2: 必须用 Java 17 吗? Java 11 可以吗?

**A**: 强烈建议使用 **Java 17 或更高版本**。

- Java 17+: ✅ 完全支持
- Java 11: ⚠️ 可能可用，但未充分测试
- Java 8: ❌ 不支持

检查 Java 版本:
```bash
java -version
```

### Q3: 插件加载失败怎么办?

**A**: 请按以下步骤排查:

1. **查看错误日志**: Extender -> Errors 标签页
2. **检查 Java 版本**: 确保 >= Java 17
3. **重新安装**: 删除插件后重新添加
4. **检查 JAR 文件**: 确认下载的文件完整且未损坏

常见错误:
```
UnsupportedClassVersionError
→ Java 版本过低，升级到 Java 17

NoClassDefFoundError
→ 依赖缺失，重新下载插件
```

### Q4: 如何更新插件?

**A**: 
- **BApp Store 安装**: 自动检测更新，点击 "Update" 即可
- **手动安装**: 下载新版本 JAR，删除旧版本后重新添加

---

## LLM 相关

### Q5: 支持哪些 LLM 提供商?

**A**: 当前支持:

| 提供商 | 模型示例 | 推荐程度 |
|--------|---------|---------|
| OpenAI | gpt-4o, gpt-4o-mini, gpt-3.5-turbo | ⭐⭐⭐⭐⭐ |
| Anthropic | claude-3-5-sonnet, claude-3-opus | ⭐⭐⭐⭐⭐ |
| Azure OpenAI | gpt-4, gpt-35-turbo | ⭐⭐⭐⭐ |
| Local (Ollama) | llama3.1, mistral, qwen | ⭐⭐⭐ |

### Q6: 使用本地模型效果如何?

**A**: 效果取决于模型参数量:

- **Llama 3.1 8B**: 基础漏洞检测可用，复杂场景较弱
- **Llama 3.1 70B**: 接近 GPT-3.5 水平，推荐
- **Qwen 2.5 14B**: 中文理解较好，适合中文应用

**优点**:
- 无需网络连接
- 无 API 费用
- 数据完全本地

**缺点**:
- 需要高性能硬件 (至少 16GB RAM)
- 速度较慢
- 准确率低于云端模型

### Q7: Token 消耗大概是多少? 成本如何?

**A**: 平均消耗 (单次完整扫描):

| 请求类型 | Token 消耗 | GPT-4 成本 | GPT-3.5 成本 |
|---------|-----------|-----------|-------------|
| 简单 GET | ~2,000 | ~$0.01 | ~$0.001 |
| 复杂 POST | ~5,000 | ~$0.03 | ~$0.003 |
| 完整 ReAct (5轮) | ~10,000 | ~$0.05 | ~$0.005 |

**每天扫描 100 次的成本**:
- GPT-4: $1 - $5
- GPT-3.5: $0.1 - $0.5
- 本地模型: $0

**节省技巧**:
- 使用 `gpt-4o-mini` 替代 `gpt-4`
- 减少 Max Iterations (默认 5 -> 3)
- 启用缓存机制
- 精细化 Scope，避免不必要的扫描

### Q8: 如何配置 OpenAI API 代理?

**A**: 在 **Base URL** 中填写代理地址:

**示例 1: 使用第三方代理**
```
Base URL: https://your-proxy.com/v1
```

**示例 2: 本地代理**
```
Base URL: http://localhost:8080/v1
```

**示例 3: Azure OpenAI**
```
Base URL: https://your-resource.openai.azure.com/
```

### Q9: API Key 如何保存? 安全吗?

**A**: 
- **存储方式**: 使用 Burp Suite 的 `saveExtensionSetting` API 加密存储
- **不会明文**: 不会以明文形式保存到配置文件
- **内存保护**: 内存中仅保留引用，不记录日志

**建议**:
- 使用专用的 API Key (不要用主账号)
- 设置消费限额 (OpenAI Dashboard)
- 定期轮换 API Key

---

## 功能使用

### Q10: Repeater 右键菜单看不到 BurpAI Agent?

**A**: 可能原因:

1. **插件未正确加载**: 检查 Extender -> Extensions 中是否有 BurpAI Agent
2. **请求类型不支持**: 确保选中的是 HTTP 请求 (不是 WebSocket)
3. **Burp 版本过低**: 升级到 2023.x

刷新插件:
```
Extender -> Extensions -> 右键 BurpAI Agent -> Reload Extension
```

### Q11: 如何只检测特定漏洞类型?

**A**: 两种方式:

**方式 1: 配置面板**
- Configuration -> Scan Policy
- 取消勾选不需要的漏洞类型

**方式 2: 右键菜单**
- Repeater -> 右键 -> Extensions -> BurpAI Agent
- 选择 **Specific: SQL Injection** 等特定选项

### Q12: 被动扫描会扫描所有流量吗?

**A**: 不会。只有满足以下条件的请求才会被扫描:

1. ✅ 在 Scope 范围内
2. ✅ 不在后缀黑名单中 (如 .jpg, .css)
3. ✅ 不在 Host 黑名单中
4. ✅ URL+参数组合在去重时间窗口外 (默认 5 分钟)

**示例**:
```
✅ 会扫描: GET /api/user?id=1001
❌ 不扫描: GET /static/image.jpg (后缀过滤)
❌ 不扫描: GET /api/user?id=1001 (5分钟内已扫描过)
```

### Q13: 如何查看 AI 的完整思维过程?

**A**: 
1. 打开 **BurpAI Agent** -> **Dashboard**
2. 点击左侧任务列表中的任务
3. 右侧 Chat View 会显示完整的思维链

也可以右键点击任务 -> **View Raw Conversation** 查看原始 JSON 对话。

### Q14: 扫描结果可以导出吗?

**A**: 可以。支持以下格式:

- **JSON**: 机器可读，包含完整数据
- **HTML**: 漂亮的网页报告，可直接分享
- **Markdown**: 适合集成到文档

导出方式:
```
Dashboard -> 右键任务 -> Export Report -> 选择格式
```

---

## 性能与优化

### Q15: 扫描速度太慢怎么办?

**A**: 优化方法:

1. **减少迭代次数**: Max Iterations 从 5 改为 3
2. **使用更快的模型**: 
   - `gpt-4` → `gpt-3.5-turbo` (快 3-5 倍)
   - `claude-3-opus` → `claude-3-haiku`
3. **精细化 Scope**: 避免扫描不必要的接口
4. **增加并发**: 在配置中调整并发任务数 (默认 1 → 3)

### Q16: 插件会不会让 Burp 变卡?

**A**: 不会。原因:

- 所有 AI 任务在 **独立线程池** 中运行
- **不阻塞** Burp 主界面
- **内存控制**: 响应体截断，避免大文件加载

如果仍感觉卡顿:
- 减少并发任务数
- 关闭被动扫描，只用主动触发
- 检查系统资源 (CPU/内存是否充足)

### Q17: 为什么有时候 LLM 响应很慢?

**A**: 可能原因:

1. **API 服务器负载**: OpenAI/Anthropic 高峰期响应较慢
2. **网络延迟**: 检查网络连接
3. **模型推理时间**: GPT-4 比 GPT-3.5 慢 2-3 倍
4. **Prompt 过长**: 响应体过大导致 Token 消耗增加

解决方案:
- 使用更快的模型
- 减小响应体截断阈值 (默认 2KB → 1KB)
- 设置超时时间 (默认 30 秒)

### Q18: 如何减少 Token 消耗?

**A**: 优化策略:

1. **响应体截断**: 默认 2KB，可调整到 1KB
2. **Header 过滤**: 只发送关键 Headers
3. **启用缓存**: 相同请求复用结果
4. **减少迭代**: Max Iterations 设置为 3
5. **精准 Scope**: 避免扫描静态资源

预期效果:
- 优化前: ~10,000 tokens/次
- 优化后: ~5,000 tokens/次

---

## 错误排查

### Q19: "Invalid API Key" 错误怎么办?

**A**: 检查清单:

1. ✅ API Key 复制是否完整 (没有多余空格)
2. ✅ API Key 是否有效 (在提供商后台验证)
3. ✅ API Key 是否有使用权限 (某些 Key 有 IP 限制)
4. ✅ Base URL 是否正确 (OpenAI: `https://api.openai.com/v1`)

测试方法:
```bash
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer YOUR_API_KEY"
```

### Q20: "JSON Parse Error" 是什么意思?

**A**: LLM 返回的不是有效的 JSON 格式。

**常见原因**:
- LLM 输出了解释性文本而不是 JSON
- 网络中断导致响应不完整
- Token 限制导致响应被截断

**解决方法**:
- 插件会自动重试并提示 LLM 修正
- 如果持续发生,尝试更换模型
- 查看 Dashboard -> View Raw Response 检查原始输出

### Q21: "Request Timeout" 怎么处理?

**A**: 原因和解决方案:

| 原因 | 解决方案 |
|------|---------|
| 网络不稳定 | 检查网络连接，使用稳定网络 |
| LLM 响应慢 | 增加超时时间到 60 秒 |
| 目标服务器慢 | 增加 HTTP 请求超时 |
| 防火墙拦截 | 检查防火墙设置 |

配置超时时间:
```
Configuration -> Advanced -> Timeout Settings
- LLM Request Timeout: 60s
- HTTP Request Timeout: 30s
```

### Q22: 扫描没有发现已知的漏洞?

**A**: 可能原因:

1. **Max Iterations 太低**: 增加到 5-7
2. **Confidence Level 太高**: 调整为 Medium 或 Low
3. **响应体被过度截断**: 增大截断阈值
4. **LLM 模型能力不足**: 使用 GPT-4 替代 GPT-3.5
5. **Prompt 不够优化**: 使用 Custom Prompt 提供更多上下文

调试方法:
- 查看完整思维链，了解 AI 的判断依据
- 手动测试确认漏洞确实存在
- 提交 Issue 附带请求/响应示例

---

## 安全与隐私

### Q23: 使用 BurpAI Agent 安全吗?

**A**: 安全措施:

- ✅ **API Key 加密存储**: 不会明文保存
- ✅ **本地处理**: 数据在本地截断和清洗后才发送
- ✅ **不记录敏感数据**: 不会记录完整的 Cookie、密码等
- ⚠️ **依赖外部 API**: 如果使用云端 LLM，数据会发送到提供商

**建议**:
- 对于极度敏感的目标，使用 **本地模型**
- 在配置中启用 **敏感数据脱敏**
- 定期审查发送给 LLM 的数据内容

### Q24: 数据会发送到哪里?

**A**: 取决于您的配置:

**使用 OpenAI/Anthropic**:
- HTTP 请求/响应 → 发送到提供商服务器
- 数据用于 AI 推理，不会用于模型训练 (根据提供商政策)

**使用本地模型 (Ollama)**:
- 数据完全本地处理
- 不会发送到互联网

**数据处理流程**:
```
原始请求 → 截断清洗 → 脱敏处理 → 发送到 LLM → 获得分析结果
```

### Q25: 会不会产生破坏性操作?

**A**: 默认情况下 **不会**。

**安全设计**:
- 只发送 **读取型** 请求 (GET、POST 查询)
- 不修改数据库或文件系统
- **RCE 检测默认关闭** (需手动启用)

**高危操作保护**:
- RCE Payload 需要用户二次确认
- 破坏性 SQL (DROP, DELETE) 默认禁止
- 文件上传/删除操作默认不测试

**建议**:
- 先在 **测试环境** 使用
- 生产环境谨慎启用 RCE 检测
- 仔细审查 AI 生成的 Payload

### Q26: 如何处理 API Key 泄露?

**A**: 立即采取以下措施:

1. **撤销泄露的 Key**: 在提供商后台删除
2. **生成新的 Key**: 创建新 API Key
3. **检查使用记录**: 查看是否有异常调用
4. **更新插件配置**: 填写新的 API Key

**预防措施**:
- 不要将 API Key 提交到 Git
- 使用环境变量或配置文件管理
- 设置每月消费限额

---

## 其他问题

### Q27: 支持团队协作吗?

**A**: 当前版本 (v1.0) 不支持。

**规划中的功能** (v2.0+):
- 配置云端同步
- 任务结果共享
- 多人协作扫描

### Q28: 可以自定义漏洞检测类型吗?

**A**: 部分支持。

**当前**:
- 可以通过 **Custom Prompt** 指定检测目标
- 不支持自定义插件式检测器

**未来计划**:
- 支持用户编写自定义检测器 (Java/Python)
- 支持自定义 Payload 库
- 支持自定义验证脚本

### Q29: 错误日志在哪里?

**A**: 两个地方:

1. **Burp 内置日志**: Extender -> Errors
2. **插件日志**: Dashboard -> Logs 标签 (如果有)

导出日志:
```
Dashboard -> Settings -> Export Logs
```

### Q30: 如何贡献代码或反馈问题?

**A**: 
- **报告 Bug**: [GitHub Issues](https://github.com/YOUR_USERNAME/burpai-agent/issues)
- **功能建议**: [GitHub Discussions](https://github.com/YOUR_USERNAME/burpai-agent/discussions)
- **贡献代码**: 参考 [贡献指南](../CONTRIBUTING.md)

---

## 还有其他问题?

- 📖 查看 [快速开始指南](./QUICK_START.md)
- 📖 查看 [使用示例](./EXAMPLES.md)
- 💬 在 [GitHub Discussions](https://github.com/YOUR_USERNAME/burpai-agent/discussions) 提问
- 📧 发送邮件至: (待补充)

我们会持续更新此文档！
